/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com', 'plus.unsplash.com', 'images.unsplash.com', 'tailwindcss.com', 'source.unsplash.com'],
  },
};

module.exports = nextConfig; 